# Reference files

These files are copied from Study Collab V1 for reference only.
Do NOT import them. Do NOT copy code from them into lib/.
Use them as:
- Business logic reference (services → understand what the feature does)
- UI design reference (screens → match the visual design)
- Rules reference (firestore.rules → adapt for Clean Architecture)

Agents: read these when building a feature to understand
prior decisions and avoid reinventing the wheel.